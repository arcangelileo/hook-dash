# =============================================================================
# HookDash — Environment Variables
# Copy this file to .env and update values for your environment.
# All variables use the HOOKDASH_ prefix.
# =============================================================================

# ---- Security ---------------------------------------------------------------
# REQUIRED in production: set a strong random secret for JWT signing.
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(64))"
HOOKDASH_SECRET_KEY=change-me-in-production-use-a-real-secret

# ---- Database ---------------------------------------------------------------
# SQLite (default, good for single-server deployments):
HOOKDASH_DATABASE_URL=sqlite+aiosqlite:///./data/hookdash.db

# ---- Application ------------------------------------------------------------
# Enable debug mode (verbose logging, auto-reload in development).
HOOKDASH_DEBUG=false

# ---- Webhook Limits ---------------------------------------------------------
# Maximum webhook body size in bytes (default: 1 MB = 1048576).
HOOKDASH_MAX_BODY_SIZE=1048576

# ---- Auth -------------------------------------------------------------------
# JWT algorithm (default: HS256). Only change if you know what you're doing.
# HOOKDASH_ALGORITHM=HS256

# JWT token expiry in minutes (default: 1440 = 24 hours).
# HOOKDASH_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ---- Plan Limits (optional overrides) ---------------------------------------
# Free tier
# HOOKDASH_FREE_MAX_ENDPOINTS=2
# HOOKDASH_FREE_MAX_REQUESTS_PER_DAY=100
# HOOKDASH_FREE_RETENTION_HOURS=24

# Pro tier
# HOOKDASH_PRO_MAX_ENDPOINTS=25
# HOOKDASH_PRO_MAX_REQUESTS_PER_DAY=10000
# HOOKDASH_PRO_RETENTION_DAYS=30

# Team tier
# HOOKDASH_TEAM_MAX_ENDPOINTS=999999
# HOOKDASH_TEAM_MAX_REQUESTS_PER_DAY=100000
# HOOKDASH_TEAM_RETENTION_DAYS=90

# ---- Docker Compose ---------------------------------------------------------
# Host port to expose (only used by docker-compose.yml).
HOOKDASH_PORT=8000
